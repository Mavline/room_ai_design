version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Переменные окружения из .env файла будут загружены через env_file
    env_file:
      - .env
    restart: always
